import { g as getApiError } from './errors-96a5615c.js';
import { j as json } from './index-2b68e648.js';
import { a as algostack } from './algostack.server-3e76bec2.js';
import 'axios';
import './shared-server-22ad612c.js';
import 'algostack';
import 'algostack/txns';

const POST = async (req) => {
  const { request } = req;
  try {
    const data = await request.json();
    const signedTxns = data.length === 1 ? new Uint8Array(Object.values(data[0])) : data.map((buffer) => new Uint8Array(Object.values(buffer)));
    const sent = await algostack.txns.submitTxns(signedTxns);
    return json(sent);
  } catch (e) {
    throw getApiError(e);
  }
};

export { POST };
//# sourceMappingURL=_server.ts-8f367bef.js.map
