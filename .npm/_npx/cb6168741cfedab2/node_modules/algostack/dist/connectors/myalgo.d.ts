import type { ConnectionSettings } from '@randlabs/myalgo-connect';
import MyAlgoConnect from '@randlabs/myalgo-connect';
import { Transaction } from 'algosdk';
import BaseConnector from './base.js';
export default class MyAlgo extends BaseConnector {
    protected connector?: MyAlgoConnect;
    constructor();
    connect(options?: ConnectionSettings): Promise<string[] | undefined>;
    sign(txns: Transaction[]): Promise<Uint8Array[] | undefined>;
}
