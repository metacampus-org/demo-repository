import type { TransactionLike } from 'algosdk';
export default class BaseConnector {
    protected connector?: any;
    connect(...args: any[]): Promise<string[] | undefined>;
    sign(txns: TransactionLike[]): Promise<Uint8Array[] | undefined>;
    disconnect(): Promise<void>;
}
