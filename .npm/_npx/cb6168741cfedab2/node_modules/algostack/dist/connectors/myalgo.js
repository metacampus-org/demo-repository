import { __awaiter } from "tslib";
import MyAlgoConnect from '@randlabs/myalgo-connect';
import BaseConnector from './base.js';
export default class MyAlgo extends BaseConnector {
    constructor() {
        super();
        this.connector = undefined;
        if (typeof window === 'undefined')
            return;
        this.connector = new MyAlgoConnect();
    }
    //
    // Connect account
    // ----------------------------------------------
    connect(options = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.connector)
                return undefined;
            try {
                const accounts = yield this.connector.connect(Object.assign({ shouldSelectOneAccount: true }, options));
                const addresses = accounts.map(account => account.address);
                return addresses;
            }
            catch (_a) {
                return undefined;
            }
        });
    }
    //
    // Sign transaction(s)
    // ----------------------------------------------
    sign(txns) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.connector)
                return undefined;
            try {
                const txnsBytes = txns.map(txn => txn.toByte());
                const result = yield this.connector.signTransaction(txnsBytes);
                const signedBytes = result.map(signed => signed.blob);
                return signedBytes;
            }
            catch (e) {
                console.error(e);
                return undefined;
            }
        });
    }
    ;
}
