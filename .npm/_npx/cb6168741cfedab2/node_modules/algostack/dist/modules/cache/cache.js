import { __awaiter } from "tslib";
import { durationStringToMs } from '../../helpers/format.js';
import { BaseModule } from '../_baseModule.js';
import { indexedDB, IDBKeyRange } from "fake-indexeddb";
import Dexie from 'dexie';
import objHash from 'object-hash';
import merge from 'lodash-es/merge.js';
import intersection from 'lodash-es/intersection.js';
import cloneDeep from 'lodash-es/cloneDeep.js';
import { wait } from '../../utils/process.js';
/**
 * Cache module
 * ==================================================
 */
export default class Cache extends BaseModule {
    get currentStores() { var _a; return ((_a = this.db) === null || _a === void 0 ? void 0 : _a.tables.map(table => table.name)) || []; }
    ;
    get isReady() { return this._isReady; }
    ;
    set isReady(value) {
        this._isReady = value;
        if (value === true)
            this.runQueue();
    }
    constructor(configs = {}) {
        super();
        this.v = 1;
        this.configs = {};
        this.stores = {};
        this.queue = [];
        this._isReady = false;
        const isBrowser = typeof window !== 'undefined';
        this.db = new Dexie(configs.namespace, isBrowser && window.indexedDB
            ? undefined
            : { indexedDB, IDBKeyRange });
        if (isBrowser) {
            window.addEventListener('unhandledrejection', this.handleError.bind(this));
        }
        this.setConfigs(configs);
    }
    setConfigs(configs) {
        super.setConfigs(configs);
        this.configs = merge({
            namespace: 'algostack',
            stores: undefined,
            expiration: {
                'default': '1h',
                'db/state': 'never',
                'indexer/asset': '1w',
                'indexer/assetBalances': '2s',
                'indexer/assetTransactions': '2s',
                'indexer/assets': '1m',
                'indexer/block': '1w',
                'indexer/transaction': '1w',
                'node/account': '10s',
                'node/teal': '6h',
                'nfd/lookup': '1h',
                'nfd/search': '1m',
                'medias/asset': '1d',
            },
            pruningInterval: undefined,
            logExpiration: false,
        }, this.configs, configs);
        if (this.stack)
            this.init(this.stack);
    }
    init(stack) {
        var _a, _b, _c;
        super.init(stack);
        const stackVersion = ((_b = (_a = this.stack) === null || _a === void 0 ? void 0 : _a.configs) === null || _b === void 0 ? void 0 : _b.version) || 1;
        let stores = Object.assign({ 'db/state': '&key' }, this.stores);
        // Query module
        if (stack.query) {
            stores = Object.assign(Object.assign({}, stores), { 
                // indexer
                'indexer/account': '&params', 'indexer/accountAssets': '&params', 'indexer/accountApplications': '&params', 'indexer/accountTransactions': '&params', 'indexer/application': '&params', 'indexer/applicationBox': '&params', 'indexer/applicationBoxes': '&params', 'indexer/asset': '&params', 'indexer/assetBalances': '&params', 'indexer/assetTransactions': '&params', 'indexer/block': '&params', 'indexer/txn': '&params', 
                // node
                'node/account': '&params', 'node/accountApplication': '&params', 'node/accountAsset': '&params', 'node/block': '&params', 'node/blockProof': '&params', 'node/blockTransactionProof': '&params', 'node/teal': '&params', 
                // search
                'indexer/applications': '&params', 'indexer/accounts': '&params', 'indexer/assets': '&params', 'indexer/txns': '&params' });
        }
        // NFDs
        if (stack.nfds) {
            stores = Object.assign(Object.assign({}, stores), { 'nfd/lookup': '&address, nfd', 'nfd/search': '&params' });
        }
        // Medias
        if (stack.medias) {
            stores = Object.assign(Object.assign({}, stores), { 'medias/asset': '&id' });
        }
        if ((_c = this.configs.stores) === null || _c === void 0 ? void 0 : _c.length) {
            const extraStores = {};
            this.configs.stores.forEach(store => {
                if (typeof store === 'string')
                    extraStores[store] = '&params';
                else
                    extraStores[store.name] = store.index || '&params';
            });
            stores = Object.fromEntries(Object.entries(Object.assign(Object.assign({}, extraStores), stores))
                .sort(([a], [b]) => a.localeCompare(b)));
        }
        this.stores = stores;
        if (this.v > stackVersion)
            return;
        this.v = stackVersion;
        // Start it!
        this.start();
        return this;
    }
    /**
    * Start Db
    * ==================================================
    */
    start() {
        return __awaiter(this, void 0, void 0, function* () {
            this.isReady = false;
            let dbVersion = 0;
            try {
                if (!this.db.isOpen())
                    yield this.db.open();
                dbVersion = this.db.verno;
                this.db.close();
            }
            catch (e) {
                // console.log(e)
            }
            // console.log('starting attempt')
            if (this.v < dbVersion)
                return;
            try {
                if (this.db.isOpen())
                    this.db.close();
                yield wait(100);
                this.db.version(this.v).stores(this.stores);
                yield this.db.open();
                this.isReady = true;
                // Auto prune
                if (this.configs.pruningInterval)
                    this.autoPrune();
            }
            catch (e) {
                console.log(e);
                this.handleError(e);
            }
        });
    }
    /**
    * Error handler
    * ==================================================
    */
    handleError(event) {
        var _a, _b;
        return __awaiter(this, void 0, void 0, function* () {
            const error = event === null || event === void 0 ? void 0 : event.reason;
            const errorNames = [error === null || error === void 0 ? void 0 : error.name];
            if ((_a = error === null || error === void 0 ? void 0 : error.inner) === null || _a === void 0 ? void 0 : _a.name)
                errorNames.push(error.inner.name);
            const fatal = [
                Dexie.errnames.Upgrade,
                Dexie.errnames.Version,
                Dexie.errnames.Schema,
            ];
            // Db is closed
            const dbIsClosed = errorNames.includes(Dexie.errnames.DatabaseClosed);
            if (dbIsClosed) {
                yield this.db.open();
            }
            const shouldClearTables = (_b = intersection(errorNames, fatal)) === null || _b === void 0 ? void 0 : _b.length;
            if (shouldClearTables) {
                console.log('An error occured while upgrading IndexedDB tables.');
                yield wait(250);
                yield this.start();
            }
        });
    }
    /**
    * Reset
    * ==================================================
    */
    clearAll() {
        return __awaiter(this, void 0, void 0, function* () {
            const isOpen = this.db.isOpen();
            if (isOpen)
                this.db.close();
            yield this.db.delete();
        });
    }
    resetDb() {
        return __awaiter(this, void 0, void 0, function* () {
            this.isReady = false;
            yield this.clearAll();
            if (this.db.isOpen())
                this.db.close();
            this.db.version(this.v).stores(this.stores);
            this.db.open();
            this.isReady = true;
        });
    }
    /**
    * Transaction Queue
    * ==================================================
    */
    commit(scope, stores, txn, frontRun = false) {
        return new Promise((resolve, reject) => {
            if (!Array.isArray(stores))
                stores = [stores];
            if (!this.isReady) {
                if (!frontRun)
                    this.queue.push({ scope, stores, txn, resolve });
                else
                    this.queue.unshift({ scope, stores, txn, resolve });
                return;
            }
            const isMissingStores = intersection(stores, this.currentStores).length < stores.length;
            if (isMissingStores) {
                this.queue.push({ scope, stores, txn, resolve });
                console.warn('Stores are missing', stores);
                return;
            }
            return this.runTxn(scope, stores, txn, resolve);
        });
    }
    runQueue() {
        if (!this.isReady)
            return;
        while (this.queue.length) {
            const txn = this.queue.shift();
            this.runTxn(txn.scope, txn.stores, txn.txn, txn.resolve);
        }
    }
    runTxn(scope, stores, txn, resolve) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!Array.isArray(stores))
                stores = [stores];
            try {
                const results = yield this.db.transaction(scope, stores, txn);
                resolve(results);
            }
            catch (e) {
                if (!this.db.isOpen)
                    this.db.open();
                this.queue.push({ scope, stores, txn, resolve });
            }
        });
    }
    /**
    * Get a collection bvased on a query
    * ==================================================
    */
    getCollection(store, query) {
        return __awaiter(this, void 0, void 0, function* () {
            let table = this.db[store];
            if (!table) {
                console.error(`Store not found (${store})`);
                return undefined;
            }
            if (query.orderBy)
                table = table.orderBy(query.orderBy);
            if (query.order && ['desc', 'DESC'].includes(query.order))
                table = table.desc();
            if (query.where) {
                const where = this.hashObjectProps(query.where);
                if (Object.keys(where).length) {
                    table = table.where(this.hashObjectProps(query.where));
                }
            }
            if (query.filter)
                table = table.filter(query.filter);
            if (!query.includeExpired)
                table = table.filter((entry) => !this.isExpired(store, entry));
            return table;
        });
    }
    /**
    * Find an entry based on its ID and the query
    * ==================================================
    */
    find(store, query) {
        return __awaiter(this, void 0, void 0, function* () {
            return this.commit('r', store, () => __awaiter(this, void 0, void 0, function* () {
                const collection = yield this.getCollection(store, query);
                if (!collection)
                    return undefined;
                //
                // Return a single entry object
                // if no limit param is defined 
                // Default behavior
                // --------------------------------------------------
                if (query.limit === undefined)
                    return yield collection.first();
                //
                // Find multiple entries
                // --------------------------------------------------    
                return yield collection.limit(query.limit).toArray();
            }));
        });
    }
    /**
    * Save an entry
    * ==================================================
    */
    save(store, data, entry) {
        return __awaiter(this, void 0, void 0, function* () {
            // if (!this.db[store]) return console.error(`Store not found (${store})`);
            entry = Object.assign(Object.assign({}, this.hashObjectProps(entry)), { data, timestamp: Date.now() });
            return this.commit('rw', store, () => __awaiter(this, void 0, void 0, function* () {
                return yield this.db[store].put(entry);
            }));
        });
    }
    bulkSave(store, entries) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!entries.length)
                return;
            const timestamp = Date.now();
            const rows = entries.map(row => (Object.assign(Object.assign({}, this.hashObjectProps(row.entry)), { data: row.data, timestamp })));
            return this.commit('rw', store, () => __awaiter(this, void 0, void 0, function* () {
                return yield this.db[store].bulkPut(rows);
            }));
        });
    }
    /**
    * Delete en entry
    * ==================================================
    */
    delete(store, query) {
        return __awaiter(this, void 0, void 0, function* () {
            return this.commit('rw', store, () => __awaiter(this, void 0, void 0, function* () {
                const collection = yield this.getCollection(store, query);
                if (!collection)
                    return 0;
                return collection.delete();
            }));
        });
    }
    /**
    * Prepare cache entry
    * ==================================================
    */
    hashObjectProps(entry = {}) {
        if (typeof entry !== 'object')
            return entry;
        const result = cloneDeep(entry);
        Object.entries(result)
            .forEach(([key, value]) => {
            if (typeof value === 'object' && !Array.isArray(value))
                return result[key] = objHash(value);
            if (value === null || value === undefined)
                return delete result[key];
        });
        return result;
    }
    /**
    * Expiration
    * ==================================================
    */
    isExpired(store, entry) {
        if (this.configs.expiration[store] === 'never')
            return false;
        const expiration = this.getExpiration(store);
        const isExpired = entry.timestamp + expiration < Date.now();
        if (isExpired && this.configs.logExpiration)
            console.warn(`[${store}] Cache entry has expired.`);
        return isExpired;
    }
    getExpiration(store) {
        const expirationStr = this.configs.expiration[store]
            || this.configs.expiration.default;
        return durationStringToMs(expirationStr);
    }
    /**
    * Pruning
    * ==================================================
    */
    prune(stores) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            const pruned = {};
            if (stores === undefined)
                stores = this.currentStores;
            else if (typeof stores === 'string')
                stores = [stores];
            if ((_a = this.configs.persist) === null || _a === void 0 ? void 0 : _a.length) {
                stores = stores.filter(store => !this.configs.persist.includes(store));
            }
            for (let i = 0; i < stores.length; i++) {
                const store = stores[i];
                // continue to the next store if it never expires
                if (this.configs.expiration[store] === 'never')
                    continue;
                const expirationLimit = Date.now() - this.getExpiration(store);
                const expired = yield this.commit('rw', store, () => __awaiter(this, void 0, void 0, function* () {
                    const table = this.db[store];
                    const expired = yield table
                        .filter(entry => entry.timestamp < expirationLimit)
                        .delete();
                    return expired;
                }), true);
                if (expired)
                    pruned[store] = expired;
            }
            return pruned;
        });
    }
    autoPrune() {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.configs.pruningInterval)
                return;
            const now = Date.now();
            const lastTimestamp = yield this.find('db/state', { where: { key: 'prunedAt' } });
            const pruneAfter = durationStringToMs(this.configs.pruningInterval);
            const nextPruning = ((lastTimestamp === null || lastTimestamp === void 0 ? void 0 : lastTimestamp.timestamp) || 0) + pruneAfter;
            if (now < nextPruning)
                return;
            const pruned = yield this.prune();
            yield this.save('db/state', null, { key: 'prunedAt' });
            console.warn('Caches pruned: ', pruned);
        });
    }
}
