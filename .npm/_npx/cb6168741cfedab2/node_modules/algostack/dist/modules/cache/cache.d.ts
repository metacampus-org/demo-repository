import { CacheConfigs, CacheEntry, CacheQuery } from './types.js';
import { BaseModule } from '../_baseModule.js';
import AlgoStack from '../../index.js';
/**
 * Cache module
 * ==================================================
 */
export default class Cache extends BaseModule {
    private db;
    private v;
    private configs;
    private stores;
    private queue;
    private get currentStores();
    private _isReady;
    private get isReady();
    private set isReady(value);
    constructor(configs?: CacheConfigs);
    setConfigs(configs: CacheConfigs): void;
    init(stack: AlgoStack): this;
    /**
    * Start Db
    * ==================================================
    */
    private start;
    /**
    * Error handler
    * ==================================================
    */
    private handleError;
    /**
    * Reset
    * ==================================================
    */
    private clearAll;
    private resetDb;
    /**
    * Transaction Queue
    * ==================================================
    */
    private commit;
    private runQueue;
    private runTxn;
    /**
    * Get a collection bvased on a query
    * ==================================================
    */
    private getCollection;
    /**
    * Find an entry based on its ID and the query
    * ==================================================
    */
    find<Q extends CacheQuery>(store: string, query: Q): Promise<Q extends {
        limit: number;
    } ? CacheEntry[] | undefined : CacheEntry | undefined>;
    /**
    * Save an entry
    * ==================================================
    */
    save(store: string, data: any, entry: CacheEntry): Promise<any>;
    bulkSave(store: string, entries: {
        data: any;
        entry: CacheEntry;
    }[]): Promise<any>;
    /**
    * Delete en entry
    * ==================================================
    */
    delete(store: string, query: CacheQuery): Promise<number>;
    /**
    * Prepare cache entry
    * ==================================================
    */
    private hashObjectProps;
    /**
    * Expiration
    * ==================================================
    */
    isExpired(store: string, entry: CacheEntry): boolean;
    private getExpiration;
    /**
    * Pruning
    * ==================================================
    */
    prune(stores?: string | string[]): Promise<{}>;
    private autoPrune;
}
