import type { TransactionParams, TxnsConfigs } from './types.js';
import { BaseModule } from '../_baseModule.js';
import algosdk, { Transaction } from 'algosdk';
import AlgoStack from '../../index.js';
import Client from '../client/index.js';
/**
 * Txns class
 * ==================================================
 */
export default class Txns extends BaseModule {
    private configs;
    protected client: Client;
    algosdk: any;
    algod: any;
    constructor(configs?: TxnsConfigs);
    setConfigs(configs: TxnsConfigs): void;
    init(stack: AlgoStack): this;
    private getBaseParams;
    prepareTxn(params: Partial<TransactionParams>): Promise<TransactionParams>;
    prepareTxns(group: Partial<TransactionParams>[]): Promise<TransactionParams[]>;
    makeTxn(params: TransactionParams): algosdk.Transaction;
    makeTxns(group: TransactionParams[]): algosdk.Transaction[];
    sendTxn(params: Partial<TransactionParams>, configs?: TxnsConfigs): Promise<Record<string, any> | undefined>;
    sendGroupedTxns(group: Partial<TransactionParams>[], configs?: TxnsConfigs): Promise<Record<string, any> | undefined>;
    sendSequencedTxns(sequence: Partial<TransactionParams>[]): Promise<Record<string, any>[] | undefined>;
    signTxns(txns: Transaction | Transaction[]): Promise<Uint8Array[] | false>;
    submitTxns(signedTxns: Uint8Array | Uint8Array[], wait?: boolean): Promise<any>;
    wait(txnId: any, maxBlocks?: number): Promise<Record<string, any>>;
}
