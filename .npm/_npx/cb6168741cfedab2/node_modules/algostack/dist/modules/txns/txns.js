import { __awaiter } from "tslib";
import { BaseModule } from '../_baseModule.js';
import algosdk, { Transaction } from 'algosdk';
import camelcaseKeys from 'camelcase-keys';
import merge from 'lodash-es/merge.js';
/**
 * Txns class
 * ==================================================
 */
export default class Txns extends BaseModule {
    constructor(configs = {}) {
        super();
        this.configs = {};
        this.setConfigs(configs);
    }
    setConfigs(configs) {
        super.setConfigs(configs);
        this.configs = merge({
            wait: true,
        }, this.configs, configs);
    }
    init(stack) {
        super.init(stack);
        this.algod = new algosdk.Algodv2(this.stack.configs.apiToken || '', this.stack.configs.apiUrl, this.stack.configs.apiPort || '');
        this.client = stack.client;
        return this;
    }
    //
    // Prepare Txns
    // ----------------------------------------------
    getBaseParams() {
        return __awaiter(this, void 0, void 0, function* () {
            const txn = yield this.algod.getTransactionParams().do();
            return txn;
        });
    }
    prepareTxn(params) {
        return __awaiter(this, void 0, void 0, function* () {
            const baseParams = yield this.getBaseParams();
            return Object.assign(Object.assign({}, baseParams), params);
        });
    }
    prepareTxns(group) {
        return __awaiter(this, void 0, void 0, function* () {
            const baseParams = yield this.getBaseParams();
            return group.map(params => (Object.assign(Object.assign({}, baseParams), params)));
        });
    }
    makeTxn(params) {
        return new algosdk.Transaction(params);
    }
    makeTxns(group) {
        return group.map(params => new algosdk.Transaction(params));
    }
    //
    // Single Transaction
    // ----------------------------------------------
    sendTxn(params, configs = this.configs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const txnParams = yield this.prepareTxn(params);
                const txn = this.makeTxn(txnParams);
                const signedTxn = yield this.signTxns(txn);
                if (!signedTxn)
                    return;
                const submited = yield this.submitTxns(signedTxn, configs.wait);
                return submited;
            }
            catch (error) {
                console.dir(error);
            }
        });
    }
    //
    // Grouped Transactions
    // ----------------------------------------------
    sendGroupedTxns(group, configs = this.configs) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const paramsGroup = yield this.prepareTxns(group);
                const txnsGroup = this.makeTxns(paramsGroup);
                const groupId = algosdk.computeGroupID(txnsGroup);
                txnsGroup.forEach(txn => {
                    txn.group = groupId;
                });
                const signedTxns = yield this.signTxns(txnsGroup);
                if (!signedTxns)
                    return undefined;
                const submited = yield this.submitTxns(signedTxns, configs.wait);
                return submited;
            }
            catch (error) {
                console.dir(error);
            }
        });
    }
    //
    // Sequenced transactions
    // ----------------------------------------------
    sendSequencedTxns(sequence) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                const paramsSequence = yield this.prepareTxns(sequence);
                const txnsSequence = this.makeTxns(paramsSequence);
                const signedTxns = yield this.signTxns(txnsSequence);
                if (!signedTxns)
                    return undefined;
                const confirmations = [];
                for (let i = 0; i < signedTxns.length; i++) {
                    const submited = yield this.submitTxns(signedTxns[i], true);
                    if (!submited.txId)
                        break;
                    confirmations.push(submited);
                }
                return confirmations;
            }
            catch (error) {
                console.dir(error);
            }
        });
    }
    //
    // Sign
    // ----------------------------------------------
    signTxns(txns) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!this.client)
                throw new Error('Client module is required');
            if (!this.client.connector)
                throw new Error('Client is not connected');
            if (!Array.isArray(txns))
                txns = [txns];
            txns = txns.map(txn => txn instanceof Transaction ? txn : new Transaction(txn));
            const signedTxns = yield this.client.connector.sign(txns);
            return signedTxns;
        });
    }
    //
    // Send transaction
    // ----------------------------------------------
    submitTxns(signedTxns, wait = this.configs.wait) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.algod.sendRawTransaction(signedTxns).do();
            if (!response.txId)
                return;
            if (!wait)
                return camelcaseKeys(response, { deep: true });
            const confirmation = yield this.wait(response.txId);
            return Object.assign(Object.assign({}, confirmation), { txId: response.txId });
        });
    }
    //
    // Wait for txn to be processed 
    // ----------------------------------------------
    wait(txnId, maxBlocks = 10) {
        return __awaiter(this, void 0, void 0, function* () {
            const confirmation = yield algosdk.waitForConfirmation(this.algod, txnId, maxBlocks);
            return camelcaseKeys(confirmation, { deep: true });
        });
    }
}
