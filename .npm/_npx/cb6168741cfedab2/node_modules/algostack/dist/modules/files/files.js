import { __awaiter } from "tslib";
import axios from 'axios';
import { MediaType } from '../../enums.js';
import { getFileType, getIpfsCid } from '../../helpers/files.js';
import { isIpfsProtocol, isUrl } from '../../helpers/strings.js';
import { BaseModule } from '../_baseModule.js';
import merge from 'lodash-es/merge.js';
/**
 * Files class
 * ==================================================
 */
export default class Files extends BaseModule {
    /**
     * Constructor
     * ==================================================
     */
    constructor(configs = {}) {
        super();
        this.configs = {};
        this.setConfigs(configs);
    }
    setConfigs(configs) {
        super.setConfigs(configs);
        this.configs = merge({
            ipfsGatewayUrl: 'https://ipfs.algonode.xyz/ipfs',
            transformUrl: undefined,
        }, this.configs, configs);
    }
    /**
     * load data from URL
     * ==================================================
     */
    getMeta(url) {
        return __awaiter(this, void 0, void 0, function* () {
            const file = {
                originalUrl: url,
                url: url,
                cid: undefined,
                type: undefined,
                mime: undefined,
                thumbnail: undefined,
                content: undefined,
            };
            if (this.configs.transformUrl)
                file.url = yield Promise.resolve(this.configs.transformUrl(url));
            if (!isUrl(file.url) && !isIpfsProtocol(file.url))
                return file;
            file.cid = getIpfsCid(file.url);
            if (file.cid)
                file.url = this.getIpfsUrl(file.cid);
            if (!file.cid && !/^(http|https):\/\//.test(file.url)) {
                file.url = `https://${file.url}`;
            }
            file.mime = yield getFileType(file.url);
            yield this.setType(file);
            return file;
        });
    }
    /**
     * Set props based on type
     * ==================================================
     */
    setType(file) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!file.mime)
                return;
            // image
            if (file.mime.startsWith('image/')) {
                file.type = MediaType.IMAGE;
                return;
            }
            // video
            if (file.mime.startsWith('video/')) {
                file.type = MediaType.VIDEO;
                return;
            }
            // json
            if (/^(application\/json|text\/plain(;?))/.test(file.mime)) {
                file.type = MediaType.JSON;
                yield this.loadJSON(file);
                return;
            }
        });
    }
    /**
     * load JSON media
     * ==================================================
     */
    loadJSON(file) {
        return __awaiter(this, void 0, void 0, function* () {
            const json = yield axios.get(file.url);
            if (!json.data)
                return;
            file.content = json.data;
        });
    }
    /**
     * Get an IPFS gateway url for a specific cid
     * ==================================================
     */
    getIpfsUrl(cid) {
        return `${this.configs.ipfsGatewayUrl}/${cid}`;
    }
}
