import { __awaiter } from "tslib";
import { Buffer } from 'buffer';
import { pRateLimit } from 'p-ratelimit';
import { utf8ToB64 } from '../../helpers/encoding.js';
import { ApiUrl } from './enums.js';
import { BaseModule } from '../_baseModule.js';
import camelcaseKeys from 'camelcase-keys';
import kebabcaseKeys from 'kebabcase-keys';
import axios from 'axios';
import merge from 'lodash-es/merge.js';
import cloneDeep from 'lodash-es/cloneDeep.js';
import objHash from 'object-hash';
/**
 * Query class
 * ==================================================
 */
export default class Query extends BaseModule {
    constructor(configs = {}) {
        super();
        this.configs = {};
        this.queue = new Map();
        // Wrap everything together
        this.lookup = {
            health: this.indexerHealth.bind(this),
            account: this.indexerAccount.bind(this),
            accountAssets: this.indexerAccountAssets.bind(this),
            accountApplications: this.indexerAccountApplications.bind(this),
            accountTransactions: this.indexerAccountTransactions.bind(this),
            application: this.indexerApplication.bind(this),
            applicationBox: this.indexerApplicationBox.bind(this),
            applicationBoxes: this.indexerApplicationBoxes.bind(this),
            asset: this.indexerAsset.bind(this),
            assetBalances: this.indexerAssetBalances.bind(this),
            assetTransactions: this.indexerAssetTransactions.bind(this),
            block: this.indexerBlock.bind(this),
            transaction: this.indexerTransaction.bind(this),
            node: {
                health: this.nodeHealth.bind(this),
                status: this.nodeStatus.bind(this),
                statusAfter: this.nodeStatusAfter.bind(this),
                account: this.nodeAccount.bind(this),
                accountApplicaction: this.nodeAccountApplication.bind(this),
                accountAsset: this.nodeAccountAsset.bind(this),
                block: this.nodeBlock.bind(this),
                dissableTeal: this.nodeDisassembleTeal.bind(this),
            }
        };
        // Wrap everything together
        this.search = {
            accounts: this.accounts.bind(this),
            applications: this.applications.bind(this),
            assets: this.assets.bind(this),
            transactions: this.transactions.bind(this),
        };
        this.setConfigs(configs);
        this.rateLimit = pRateLimit({
            interval: 1000,
            rate: this.configs.rps,
            concurrency: 25,
        });
    }
    setConfigs(configs) {
        this.configs = merge({
            rps: 50,
        }, this.configs, configs);
    }
    init(stack) {
        super.init(stack);
        this.cache = stack.cache;
        return this;
    }
    /**
    * Query wrapper
    * ==================================================
    */
    query(queryOptions) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            var _a;
            const { base = ApiUrl.INDEXER, endpoint, store, params: originalParams = {}, } = queryOptions;
            let data;
            // Prepare Params
            let { params, url } = this.mergeUrlAndParams(endpoint, originalParams);
            if (params.limit === -1)
                delete params.limit;
            if (params.refreshCache !== undefined)
                delete params.refreshCache;
            if (params.noCache !== undefined)
                delete params.noCache;
            const addons = params.addons;
            const filter = params.filter;
            const cleanParams = this.cleanParams(params);
            const encodedParams = this.encodeParams(cleanParams);
            const reqParams = kebabcaseKeys(encodedParams, { deep: true });
            reqParams.url = `${base}${url}`;
            if (filter)
                delete params.filter;
            if (addons)
                delete params.addons;
            // get cached data
            if (this.cache && store && !originalParams.refreshCache && !originalParams.noCache) {
                const cached = yield this.cache.find(store, { where: { params: reqParams } });
                if (cached) {
                    data = cached.data;
                    return resolve(data);
                }
            }
            const hash = objHash({ base, endpoint, originalParams });
            this.pushToQueue(hash, resolve);
            if (((_a = this.queue.get(hash)) === null || _a === void 0 ? void 0 : _a.length) > 1)
                return;
            data = yield this.fetch(`${this.stack.configs[base]}${url}`, reqParams);
            data = camelcaseKeys(data, { deep: true });
            if (addons)
                yield this.applyAddons(data, addons);
            if (filter)
                data = this.applyFilter(data, filter);
            // Loop
            let i = 0;
            while (this.shouldFetchNext(data, originalParams) && i < 20) {
                i++;
                let nextData = yield this.fetch(`${this.stack.configs[base]}${url}`, Object.assign(Object.assign({}, reqParams), { next: data.nextToken }));
                delete data.nextToken;
                nextData = camelcaseKeys(nextData, { deep: true });
                if (addons)
                    yield this.applyAddons(nextData, addons);
                if (filter)
                    nextData = this.applyFilter(nextData, filter);
                // merge arrays, including possible new 'next-token'
                Object.entries(nextData).forEach(([key, value]) => {
                    if (Array.isArray(value) && data[key])
                        data[key] = [...data[key], ...value];
                    else
                        data[key] = value;
                });
            }
            // cache result
            if (this.cache && store && !originalParams.noCache && !data.error) {
                yield this.cache.save(store, data, { params: reqParams });
            }
            this.resolveQueue(hash, data);
        }));
    }
    /**
    * Iterate throught results
    * ==================================================
    */
    applyAddon(data, addons) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!Array.isArray(data))
                data = [data];
            yield Promise.all(data.reduce((promises, dataset) => ([
                ...promises,
                ...addons.map(addon => addon(dataset)),
            ]), []));
        });
    }
    applyAddons(data, addons) {
        return __awaiter(this, void 0, void 0, function* () {
            // addons are applied to all data
            if (Array.isArray(addons)) {
                yield this.applyAddon(data, addons);
                return;
            }
            // addons are applied to specified props
            const dataKeys = Object.keys(data)
                .filter(key => addons.has(key));
            if (!dataKeys.length)
                return;
            yield Promise.all(dataKeys.map(key => this.applyAddon(data[key], addons.get(key))));
        });
    }
    applyFilter(data, filterFn) {
        if (Array.isArray(data))
            return data.filter(filterFn);
        Object.entries((data)).forEach(([key, value]) => {
            if (Array.isArray(value))
                data[key] = value.filter(filterFn);
        });
        return data;
    }
    getResultsQty(data) {
        if (Array.isArray(data)) {
            return data.length;
        }
        return Object.values(data)
            .filter(value => Array.isArray(value))
            .reduce((total, value) => (Math.max(value.length, total)), 0);
    }
    shouldFetchNext(data, params) {
        if (params.limit === -1 && data.nextToken)
            return true;
        if (params.filter
            && params.limit
            && data.nextToken
            && this.getResultsQty(data) < params.limit)
            return true;
        return false;
    }
    /**
    *  PARAMS
    * ==================================================
    */
    mergeUrlAndParams(url, params) {
        if (!url)
            return { url: '/', params };
        const unusedParams = {};
        if (params) {
            Object.entries(params)
                .forEach(([key, value]) => {
                if (url && url.indexOf(':' + key) > -1) {
                    url = url.replace(':' + key, String(value));
                }
                else {
                    unusedParams[key] = value;
                }
            });
        }
        return { url, params: unusedParams };
    }
    cleanParams(params) {
        Object.entries(params).forEach(([key, value]) => {
            if (value === undefined)
                delete params[key];
        });
        return params;
    }
    encodeParams(params) {
        if (typeof params.notePrefix === 'string') {
            params.notePrefix = utf8ToB64(params.notePrefix);
        }
        return params;
    }
    /**
    * Queue
    * ==================================================
    */
    pushToQueue(hash, resolve) {
        const resolvers = this.queue.get(hash) || [];
        resolvers.push(resolve);
        this.queue.set(hash, resolvers);
    }
    resolveQueue(hash, data) {
        const resolvers = this.queue.get(hash) || [];
        resolvers.forEach(resolve => resolve(data));
        this.queue.delete(hash);
    }
    /**
    * Fetch data
    * ==================================================
    */
    fetch(url, params = {}, client = this.configs.client || axios) {
        return __awaiter(this, void 0, void 0, function* () {
            try {
                if (url.indexOf(':id') > -1) {
                    return { error: {
                            url,
                            message: 'Url is invalid',
                        } };
                }
                params = cloneDeep(params);
                const method = params.method
                    ? String(params.method).toUpperCase()
                    : 'GET';
                if (params.method)
                    delete params.method;
                const headers = params.headers;
                if (params.headers)
                    delete params.headers;
                if (params.url)
                    delete params.url;
                if (params.addons)
                    delete params.addons;
                if (params.filter)
                    delete params.filter;
                const data = (params === null || params === void 0 ? void 0 : params.data) || params;
                const response = yield this.rateLimit(() => client.request({
                    url,
                    method,
                    headers,
                    params: method === 'GET' ? data : undefined,
                    data: method !== 'GET' ? data : undefined,
                }));
                return response.data;
            }
            catch (e) {
                return { error: e.toJSON ? e.toJSON() : e };
            }
        });
    }
    /**
    * Lookup methods
    * ==================================================
    */
    // status
    indexerHealth() {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.fetch(`${this.stack.configs.indexerUrl}/health`);
            return camelcaseKeys(response, { deep: true });
        });
    }
    // accounts
    indexerAccount(accountId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/accounts/:id`,
                store: 'indexer/account',
                params: Object.assign(Object.assign({}, params), { id: accountId }),
            });
        });
    }
    indexerAccountTransactions(accountId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/accounts/:id/transactions`,
                store: 'indexer/accountTransactions',
                params: Object.assign(Object.assign({}, params), { id: accountId }),
            });
        });
    }
    indexerAccountAssets(accountId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/accounts/:id/assets`,
                store: 'indexer/accountAssets',
                params: Object.assign(Object.assign({}, params), { id: accountId }),
            });
        });
    }
    indexerAccountApplications(accountId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/accounts/:id/apps-local-state`,
                store: 'indexer/accountApplications',
                params: Object.assign(Object.assign({}, params), { id: accountId }),
            });
        });
    }
    // app
    indexerApplication(appId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/applications/:id`,
                store: 'indexer/application',
                params: Object.assign(Object.assign({}, params), { id: appId }),
            });
        });
    }
    indexerApplicationBox(appId, boxName, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/applications/:id/boxes`,
                store: 'indexer/applicationBoxes',
                params: Object.assign(Object.assign({}, params), { id: appId, name: `b64:${boxName}` }),
            });
        });
    }
    indexerApplicationBoxes(appId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/applications/:id/boxes`,
                store: 'indexer/applicationBoxes',
                params: Object.assign(Object.assign({}, params), { id: appId }),
            });
        });
    }
    // asset
    indexerAsset(assetId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/assets/:id`,
                store: 'indexer/asset',
                params: Object.assign(Object.assign({}, params), { id: assetId }),
            });
        });
    }
    indexerAssetBalances(assetId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/assets/:id/balances`,
                store: 'indexer/assetBalances',
                params: Object.assign(Object.assign({}, params), { id: assetId }),
            });
        });
    }
    indexerAssetTransactions(assetId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/assets/:id/transactions`,
                store: 'indexer/assetTransactions',
                params: Object.assign(Object.assign({}, params), { id: assetId }),
            });
        });
    }
    // block
    indexerBlock(round, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/blocks/:id`,
                store: 'indexer/block',
                params: Object.assign(Object.assign({}, params), { id: round }),
            });
        });
    }
    // transaction
    indexerTransaction(id, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/transactions/:id`,
                store: 'indexer/txn',
                params: Object.assign(Object.assign({}, params), { id: id }),
            });
        });
    }
    /**
    * Node queries (Algod API)
    * ==================================================
    */
    nodeHealth() {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.fetch(`${this.stack.configs.apiUrl}/health`);
            return camelcaseKeys(response, { deep: true });
        });
    }
    nodeStatus() {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.fetch(`${this.stack.configs.apiUrl}/v2/status`);
            return camelcaseKeys(response, { deep: true });
        });
    }
    nodeStatusAfter(block) {
        return __awaiter(this, void 0, void 0, function* () {
            const response = yield this.fetch(`${this.stack.configs.apiUrl}/v2/status/wait-for-block-after/${block}`);
            return camelcaseKeys(response, { deep: true });
        });
    }
    nodeAccount(accountId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                base: ApiUrl.NODE,
                endpoint: `/v2/accounts/:id`,
                store: 'node/account',
                params: Object.assign(Object.assign({}, params), { id: accountId }),
            });
        });
    }
    nodeAccountApplication(accountId, appId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                base: ApiUrl.NODE,
                endpoint: `/v2/accounts/:id/applications/:appId`,
                store: 'node/accountApplication',
                params: Object.assign(Object.assign({}, params), { id: accountId, appId }),
            });
        });
    }
    nodeAccountAsset(accountId, assetId, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                base: ApiUrl.NODE,
                endpoint: `/v2/accounts/:id/assets/:assetId`,
                store: 'node/accountAsset',
                params: Object.assign(Object.assign({}, params), { id: accountId, assetId }),
            });
        });
    }
    nodeBlock(round, params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                base: ApiUrl.NODE,
                endpoint: `/v2/blocks/:id`,
                store: 'node/block',
                params: Object.assign(Object.assign({}, params), { id: round }),
            });
        });
    }
    nodeDisassembleTeal(b64) {
        return __awaiter(this, void 0, void 0, function* () {
            if (!(b64 === null || b64 === void 0 ? void 0 : b64.length))
                return undefined;
            const programBuffer = Buffer.from(b64, 'base64');
            const response = yield this.custom(`${this.stack.configs[ApiUrl.NODE]}/v2/teal/disassemble`, 'node/teal', {
                method: 'POST',
                refreshCache: true,
                headers: { 'Content-Type': 'application/x-binary' },
                data: programBuffer,
            });
            return response === null || response === void 0 ? void 0 : response.result;
        });
    }
    /**
    * Search
    * ==================================================
    */
    accounts(params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/accounts`,
                store: 'indexer/accounts',
                params,
            });
        });
    }
    applications(params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/applications`,
                store: 'indexer/applications',
                params,
            });
        });
    }
    assets(params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/assets`,
                store: 'indexer/assets',
                params,
            });
        });
    }
    transactions(params = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            return yield this.query({
                endpoint: `/v2/transactions`,
                store: 'indexer/txns',
                params,
            });
        });
    }
    /**
    * Custom queries
    * ==================================================
    */
    custom(apiUrl, store, originalParams = {}, client) {
        return new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            var _a;
            let data;
            let { params, url } = this.mergeUrlAndParams(apiUrl, originalParams);
            if (params.refreshCache !== undefined)
                delete params.refreshCache;
            if (params.noCache !== undefined)
                delete params.noCache;
            params.url = url;
            // get cached data
            if (this.cache && store && !originalParams.refreshCache && !originalParams.noCache) {
                const cached = yield this.cache.find(store, { where: { params } });
                if (cached) {
                    data = cached.data;
                    return resolve(data);
                }
            }
            const hash = objHash({ apiUrl, originalParams });
            this.pushToQueue(hash, resolve);
            if (((_a = this.queue.get(hash)) === null || _a === void 0 ? void 0 : _a.length) > 1)
                return;
            data = yield this.fetch(url, params, client);
            // cache result
            if (this.cache && store && !originalParams.noCache && !data.error) {
                yield this.cache.save(store, data, { params });
            }
            this.resolveQueue(hash, data);
        }));
    }
}
