import { __awaiter } from "tslib";
import axios from 'axios';
import throttle from 'lodash-es/throttle.js';
import chunk from 'lodash-es/chunk.js';
import { isAddress, isDomain } from '../../helpers/strings.js';
import { BaseModule } from '../_baseModule.js';
import merge from 'lodash-es/merge.js';
/**
* NFDs module
* ==================================================
*/
export default class NFDs extends BaseModule {
    constructor(configs = {}) {
        super();
        this.configs = {};
        // Run lookup batches
        this.batchLookups = throttle(() => __awaiter(this, void 0, void 0, function* () {
            const queue = Object.keys(this.lookupQueue);
            const batches = chunk(queue, 20);
            batches.forEach((addresses) => __awaiter(this, void 0, void 0, function* () {
                let results = {};
                const addressesQueryString = `address=${addresses.join('&address=')}`;
                try {
                    const response = yield axios.get(`${this.configs.nfdApiUrl}/nfd/lookup?${addressesQueryString}`, {
                        params: { view: 'full' }
                    });
                    if (response === null || response === void 0 ? void 0 : response.data)
                        results = response.data;
                }
                catch (_a) { }
                // loop each address in batch to map it with results
                addresses.forEach((address) => __awaiter(this, void 0, void 0, function* () {
                    var _b;
                    let nfd = results[address];
                    nfd = this.prepareResult(results[address]);
                    // trigger current stack
                    if ((_b = this.lookupQueue[address]) === null || _b === void 0 ? void 0 : _b.length) {
                        this.lookupQueue[address].forEach(({ full, resolve }) => resolve(full ? nfd : nfd === null || nfd === void 0 ? void 0 : nfd.name));
                        delete this.lookupQueue[address];
                    }
                    // save cache
                    if (this.cache) {
                        yield this.cache.save('nfd/lookup', nfd, { address, nfd: nfd === null || nfd === void 0 ? void 0 : nfd.name });
                    }
                }));
            }));
        }), 200);
        // batch whois
        this.batchWhois = throttle(() => __awaiter(this, void 0, void 0, function* () {
            const queue = Object.keys(this.whoisQueue);
            queue.forEach((name) => __awaiter(this, void 0, void 0, function* () {
                var _c;
                let nfd;
                try {
                    const { data } = yield axios.get(`${this.configs.nfdApiUrl}/nfd/${name.toLocaleLowerCase()}`, {
                        params: { view: 'full' }
                    });
                    nfd = this.prepareResult(data);
                }
                catch (_d) { }
                // trigger current stack
                if ((_c = this.whoisQueue[name]) === null || _c === void 0 ? void 0 : _c.length) {
                    this.whoisQueue[name].forEach(({ resolve }) => resolve(nfd));
                    delete this.whoisQueue[name];
                }
            }));
        }), 200);
        this.lookupQueue = {};
        this.whoisQueue = {};
        this.setConfigs(configs);
    }
    setConfigs(configs) {
        this.configs = merge({
            nfdApiUrl: 'https://api.nf.domains',
        }, this.configs, configs);
    }
    init(stack) {
        super.init(stack);
        this.cache = stack.cache;
        return this;
    }
    /**
    * LOOKUPS
    * Get NFDs for a given address (batched lookup)
    * ==================================================
    */
    lookup(address, full) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
                if (!isAddress(address))
                    return resolve(undefined);
                // get cache
                if (this.cache) {
                    const cached = yield this.cache.find('nfd/lookup', { where: { address } });
                    if (cached)
                        return resolve(full ? cached.data : cached.nfd);
                }
                // check if a task is currently fetching this address
                if (!this.lookupQueue[address])
                    this.lookupQueue[address] = [];
                this.lookupQueue[address].push({ full, resolve });
                // trigger throttled fetch
                this.batchLookups();
            }));
        });
    }
    // Legacy method
    getNFDs(address, full) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
                const result = yield this.lookup(address, full);
                resolve(result ? [result] : []);
            }));
        });
    }
    /**
    * WHOIS
    * Reverse lookups
    * ==================================================
    */
    whois(name) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
                if (!isDomain(name))
                    return resolve(undefined);
                // get cache
                if (this.cache) {
                    const cached = yield this.cache.find('nfd/lookup', { where: { nfd: name } });
                    if (cached)
                        return resolve(cached.data);
                }
                // check if a task is currently fetching this address
                if (!this.whoisQueue[name])
                    this.whoisQueue[name] = [];
                this.whoisQueue[name].push({ resolve });
                // trigger throttled fetch
                this.batchWhois();
            }));
        });
    }
    // Legacy methods
    getNFD(nfd) {
        return __awaiter(this, void 0, void 0, function* () {
            return this.whois(nfd);
        });
    }
    getAddress(name) {
        return __awaiter(this, void 0, void 0, function* () {
            return new Promise((resolve) => __awaiter(this, void 0, void 0, function* () {
                const nfd = yield this.whois(name);
                resolve(nfd === null || nfd === void 0 ? void 0 : nfd.depositAccount);
            }));
        });
    }
    /**
    * Search
    * ==================================================
    */
    search(str, originalParams) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            const params = Object.assign({}, originalParams);
            if (params.refreshCache !== undefined)
                delete params.refreshCache;
            if (params.noCache !== undefined)
                delete params.noCache;
            params.substring = str;
            params.view = 'full';
            // get cache
            if (this.cache && !originalParams.refreshCache && !originalParams.noCache) {
                const cached = yield this.cache.find('nfd/search', { where: { params } });
                if (cached)
                    return cached.data;
            }
            let results = [];
            try {
                const response = yield axios.get(`${this.configs.nfdApiUrl}/nfd/v2/search`, {
                    params: Object.assign(Object.assign({}, params), { substring: str, view: 'full' })
                });
                const nfds = (_a = response.data) === null || _a === void 0 ? void 0 : _a.nfds;
                if (nfds.length) {
                    results = nfds.map(this.prepareResult.bind(this));
                }
            }
            catch (_b) { }
            // save cache
            if (this.cache && !originalParams.noCache) {
                yield this.cache.save('nfd/search', results, { params });
            }
            return results;
        });
    }
    /**
    * Helpers
    * ==================================================
    */
    prepareResult(nfd) {
        var _a, _b, _c, _d, _e;
        if (!nfd)
            return undefined;
        // add score
        nfd.score = this.getNFDScore(nfd);
        // verified doesnt get cleared up 
        // so if both are there, take userdefined
        if (nfd.properties.verified)
            nfd.avatar = ((_b = (_a = nfd.properties) === null || _a === void 0 ? void 0 : _a.userDefined) === null || _b === void 0 ? void 0 : _b.avatar) || ((_c = nfd.properties.verified) === null || _c === void 0 ? void 0 : _c.avatar);
        else
            nfd.avatar = (_e = (_d = nfd.properties) === null || _d === void 0 ? void 0 : _d.userDefined) === null || _e === void 0 ? void 0 : _e.avatar;
        return nfd;
    }
    getNFDScore(nfd) {
        let score = 0;
        const { properties: props } = nfd;
        if (!props)
            return score;
        ['avatar', 'domain', 'email', 'twitter', 'discord'].forEach(contact => {
            var _a, _b;
            if ((_a = props === null || props === void 0 ? void 0 : props.verified) === null || _a === void 0 ? void 0 : _a[contact])
                score += 1.75;
            if ((_b = props === null || props === void 0 ? void 0 : props.userDefined) === null || _b === void 0 ? void 0 : _b[contact])
                score += 1;
        });
        return score;
    }
}
