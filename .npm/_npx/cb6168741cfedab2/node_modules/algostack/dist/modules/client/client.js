import { __awaiter } from "tslib";
import { Connector } from '../../enums.js';
import { BaseModule } from '../_baseModule.js';
import Storage from '../../utils/storage.js';
import MyAlgo from '../../connectors/myalgo.js';
import Pera from '../../connectors/pera.js';
import Defly from '../../connectors/defly.js';
import Mnemonic from '../../connectors/mnemonic.js';
import merge from 'lodash-es/merge.js';
/**
 * Client class
 * ==================================================
 */
export default class Client extends BaseModule {
    constructor(configs) {
        super();
        this.configs = {};
        this._connector = undefined;
        this._connected = undefined;
        this._addresses = [];
        this.setConfigs(configs);
    }
    setConfigs(configs) {
        this.configs = merge({
            namespace: 'algostack',
            persistConnection: true,
        }, this.configs, configs);
        this.storage = new Storage(this.configs.namespace);
        this.optionallyLoadPersisted();
    }
    init(stack) {
        super.init(stack);
        if (this.configs.mnemonic)
            this.connectMnemonic(this.configs.mnemonic);
        return this;
    }
    //
    // Accessors (readonly outside of class)
    // ----------------------------------------------
    get connector() {
        return this._connector;
    }
    get connected() {
        return this._connected;
    }
    get addresses() {
        return this._addresses;
    }
    //
    // Connect
    // ----------------------------------------------
    connect(connector, options) {
        return __awaiter(this, void 0, void 0, function* () {
            this.useConnector(connector);
            if (!this._connector)
                return undefined;
            const addresses = yield this._connector.connect(options);
            if (addresses) {
                this._addresses = addresses;
                this.optionallyPersist();
                return this._addresses;
            }
            else {
                yield this.disconnect();
                return undefined;
            }
        });
    }
    disconnect() {
        return __awaiter(this, void 0, void 0, function* () {
            if (this._connector)
                yield this._connector.disconnect();
            this._connected = undefined;
            this._connector = undefined;
            this._addresses = [];
            this.optionallyPersist();
        });
    }
    //
    // Connectors
    // --------------------------------------------------
    useConnector(connector) {
        if (connector === Connector.MYALGO)
            this.useMyAlgo();
        else if (connector === Connector.PERA)
            this.usePera();
        else if (connector === Connector.DEFLY)
            this.useDefly();
        else if (connector === Connector.MNEMONIC)
            this.useMnemonic();
    }
    //
    // Mnemonic
    // ----------------------------------------------
    connectMnemonic(mnemonic = '') {
        return __awaiter(this, void 0, void 0, function* () {
            const connected = yield this.connect(Connector.MNEMONIC, mnemonic);
            return connected;
        });
    }
    useMnemonic(mnemonic = '') {
        this._connected = Connector.MNEMONIC;
        this._connector = new Mnemonic();
        return this._connector.ready;
    }
    //
    // MYALGO
    // ----------------------------------------------
    connectMyAlgo(options = {}) {
        return __awaiter(this, void 0, void 0, function* () {
            const connected = yield this.connect(Connector.MYALGO, options);
            return connected;
        });
    }
    useMyAlgo() {
        this._connected = Connector.MYALGO;
        this._connector = new MyAlgo();
    }
    //
    // PERA
    // ----------------------------------------------
    connectPera() {
        return __awaiter(this, void 0, void 0, function* () {
            const connected = yield this.connect(Connector.PERA);
            return connected;
        });
    }
    usePera() {
        this._connected = Connector.PERA;
        this._connector = new Pera();
    }
    //
    // DEFLY
    // ----------------------------------------------
    connectDefly() {
        return __awaiter(this, void 0, void 0, function* () {
            const connected = yield this.connect(Connector.DEFLY);
            return connected;
        });
    }
    useDefly() {
        this._connected = Connector.DEFLY;
        this._connector = new Defly();
    }
    //
    // Persist connection
    // ----------------------------------------------
    optionallyPersist() {
        if (!this.configs.persistConnection)
            return;
        this.storage.set('client', {
            connected: this._connected,
            addresses: this._addresses,
        });
    }
    optionallyLoadPersisted() {
        if (!this.configs.persistConnection)
            return;
        const persisted = this.storage.get('client');
        if (!persisted || !persisted.connected)
            return;
        if (persisted.addresses)
            this._addresses = persisted.addresses;
        this.useConnector(persisted.connected);
    }
}
