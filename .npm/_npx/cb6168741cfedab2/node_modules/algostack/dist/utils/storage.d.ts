export default class Storage {
    private namespace;
    available: boolean;
    data: Object;
    constructor(namespace: string);
    preload(): boolean;
    get(key: string): any;
    set(key: string, value: any): boolean;
}
